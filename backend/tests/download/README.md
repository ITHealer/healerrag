# Marker parser incremental versions

- v0: baseline reconstructed from your first two files.
- v1: env/cache fix only.
- v2: v1 + OCR langs + batch multiplier.
- v3: v2 + auto profile detection + per-profile converter / force_ocr.
